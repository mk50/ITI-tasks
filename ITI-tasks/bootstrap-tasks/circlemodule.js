class Circle  extends shape{
pi=3.14
radius=2;
constructor(_pi,_radius){
    super(color)
    this.pi=_pi;
    this.radius=_radius;

}
Circlearea(){
    var area=pi*this.radius*this.radius;
}
getarea(){
    this.Circlearea();
    this. shapedraw();
}
  areaci=new Circle("the area is + " + this.getarea); 
}